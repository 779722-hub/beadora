---
---
<section class="contacts">
  <div class="container">
    <div class="grid">
      <div class="info reveal">
        <span class="eyebrow">Контакты</span>
        <h2>Мы в Астане</h2>

        <div class="rows">
          <div class="row">
            <div class="label">Адрес</div>
            <div class="value">
              Республика Казахстан, г. Астана,<br />
              ул. Брусиловского, 5<br />
              Магазин «ADORA»
            </div>
          </div>
          <div class="row">
            <div class="label">Телефон</div>
            <div class="value">
              <a href="tel:+77787806540">+7 (778) 780-65-40</a>
            </div>
          </div>
          <div class="row">
            <div class="label">Мессенджеры</div>
            <div class="value">
              <a href="https://wa.me/77787806540" target="_blank" rel="noopener">WhatsApp</a> ·
              <a href="https://www.instagram.com/beadora.kz/" target="_blank" rel="noopener">Instagram</a>
            </div>
          </div>
          <div class="row">
            <div class="label">Часы работы</div>
            <div class="value">Ежедневно, 10:00 – 20:00 (Астана)</div>
          </div>
        </div>
      </div>

      <div class="map reveal">
        <iframe
          src="https://yandex.ru/map-widget/v1/?ll=71.45%2C51.12&z=14"
          width="100%"
          height="420"
          frameborder="0"
          title="Карта магазина Beadora в Астане"
          loading="lazy"
        ></iframe>
      </div>
    </div>
  </div>
</section>

<style>
  .grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: clamp(1.5rem, 3vw, 3rem);
    align-items: stretch;
  }
  @media (max-width: 820px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
  .eyebrow {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 0.75rem;
  }
  h2 {
    margin-bottom: 2rem;
  }
  .rows {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
  .row {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 1rem;
    align-items: start;
  }
  @media (max-width: 540px) {
    .row {
      grid-template-columns: 1fr;
    }
  }
  .label {
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-muted);
  }
  .value {
    color: var(--text);
    line-height: 1.55;
  }
  .value a {
    color: var(--accent);
  }
  .map {
    border-radius: 20px;
    overflow: hidden;
    border: 1px solid var(--border);
    min-height: 420px;
    background: var(--surface);
  }
  .map iframe {
    display: block;
    height: 100%;
    min-height: 420px;
  }
</style>
